name: Call update changelog

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Target version; leave empty to auto bump"
        required: false
        type: string
      base_branch:
        description: "Base branch to read and target"
        required: false
        default: master
        type: string
      distribution:
        description: "Changelog distribution"
        required: false
        default: unstable
        type: string
      create_pr:
        description: "Create a pull request after generating changelog"
        required: false
        default: true
        type: boolean

jobs:
  update_changelog:
    uses: peeweep-test/.github/.github/workflows/update-changelog.yml@main
    with:
      version: ${{ inputs.version }}
      base_branch: ${{ inputs.base_branch }}
      distribution: ${{ inputs.distribution }}
      create_pr: ${{ inputs.create_pr }}
    secrets: inherit

